zone_one_search.close_quickview(""); 
$('<div>').attr('id', 'quick_edit_panel').html("<%=j render "quick_edit"%>").dialog({
	title: "<%=j render "quick_edit_title" %>",
	width: 420,
	resizable: false,
	close: function() {
		$(this).remove();
	}
});

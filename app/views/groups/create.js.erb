zone_one_base.close_quickview("<%=j @group.name + " created" %>");
var last = $('#groups table tr:last').attr('class');
$('#groups table').append("<%=j render "group", :group => @group %>");
if(last == "even") {
	$('#groups table tr:last').addClass('odd').removeClass('even');
}

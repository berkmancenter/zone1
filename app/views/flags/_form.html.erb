<% flags.each do |flag| %>
  <%= f.fields_for :flaggings, stored_file.flaggings.build(:flag_id => flag.id) do |flaggings_form| %>
    <%= render "flaggings/form", :f => flaggings_form, :stored_file => stored_file %>
  <% end %>
<% end %>
